<UserControl x:Class="Backdrop.NET.WidgetContainer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Border Background="#CC1E1E1E" CornerRadius="8" BorderBrush="#33FFFFFF" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Thumb x:Name="DragGrip"
                   Grid.Row="0"
                   Height="20"
                   Cursor="SizeAll"
                   DragDelta="DragGrip_OnDragDelta">
                <Thumb.Template>
                    <ControlTemplate>
                        <Border Background="#22FFFFFF" CornerRadius="8,8,0,0"/>
                    </ControlTemplate>
                </Thumb.Template>
            </Thumb>
            <ContentPresenter x:Name="WidgetContentHost" Grid.Row="1" Margin="10"/>
        </Grid>
    </Border>
</UserControl>
